This project uses two separate environment files, one per service.

Before running with Docker Compose, create these two real files by copying the examples:

  cp backend/.env.example backend/.env
  cp frontend/.env.example frontend/.env

Then edit backend/.env and set a real value for JWT_SECRET.
All other variable keys and their meanings are documented inside backend/.env.example and frontend/.env.example.
